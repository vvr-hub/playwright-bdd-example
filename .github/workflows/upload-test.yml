name: Upload Test

on:
  push:
    branches: [ "main" ]

jobs:
  upload:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Upload test file
        uses: actions/upload-artifact@v3 # or v2
        with:
          name: test-artifact
          path: test.txt